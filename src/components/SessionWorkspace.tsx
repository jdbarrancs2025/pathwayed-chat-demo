/**
 * Workspace pane — scaffold only. The notepad, flashcards, and homework-upload
 * tools arrive in the next prompt.
 */
export function SessionWorkspace() {
  return (
    <div className="wspane">
      <div className="wsbody">
        <p className="wshint">
          Your workspace — notepad, flashcards, and homework upload tools arrive next.
        </p>
      </div>
    </div>
  )
}
