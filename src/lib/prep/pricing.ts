/**
 * Display price for a test-prep module add-on: $19 per student, per month. This
 * is UI copy only — it must match the configured Stripe prep prices
 * (STRIPE_PRICE_<MODULE>_PREP_MONTHLY), the same way billing.ts PLANS mirrors the
 * plan prices.
 */
export const PREP_PRICE_MONTHLY = 19
